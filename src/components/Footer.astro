---
const socialLinks = [
  { href: '#', icon: '📘', label: 'Facebook' },
  { href: '#', icon: '📱', label: 'Twitter' },
  { href: '#', icon: '📸', label: 'Instagram' },
  { href: '#', icon: '📌', label: 'Pinterest' }
];
---

<footer class="bg-deep-blue text-white mt-auto">
  <div class="max-w-7xl mx-auto px-4 py-8">
    <div class="flex flex-col md:flex-row justify-between items-center">
      <div class="mb-4 md:mb-0">
        <h3 class="font-display text-soft-gold text-xl">EnchantedTales</h3>
        <p class="text-sm mt-2">Where magic comes to life through stories</p>
      </div>
      <div class="flex space-x-4">
        {socialLinks.map(link => (
          <a
            href={link.href}
            class="hover:text-soft-gold transition-colors duration-200"
            aria-label={link.label}
          >
            <span class="text-2xl">{link.icon}</span>
          </a>
        ))}
      </div>
    </div>
    <div class="mt-8 text-center text-sm">
      <p>&copy; {new Date().getFullYear()} EnchantedTales. All rights reserved.</p>
    </div>
  </div>
</footer>